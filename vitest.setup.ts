// Shared test setup. Currently empty — kept as the registration point
// for future helpers (snapshot serializers, custom matchers, harness
// globals). Each test file imports utilities directly rather than
// relying on global registration, so changes here don't churn existing
// snapshots.
export {};
