dist/
.vite/
*.tsbuildinfo

# Generated by scripts/build-llms-full.ts at predev/prebuild.
public/llms-full.txt

# TanStack Start / Vite SSR build output.
.output/
.nitro/
.tanstack/

# src/routeTree.gen.ts is committed (not ignored): it makes `pnpm typecheck`
# work on a fresh clone and in CI without a prior build. The Vite plugin
# regenerates it on every `vite dev`/`vite build`.
